<Page x:Class="WpfApp2.Pages.AdminPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp2.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AdminPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="127*"/>
            <RowDefinition Height="323*"/>
        </Grid.RowDefinitions>
        <DataGrid x:Name="UserDataGrid" AutoGenerateColumns="False" ColumnWidth="*" CanUserAddRows="False" Margin="0,0,0,116" Grid.RowSpan="2">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding id}"/>
                <DataGridTextColumn Header="Логин" Binding="{Binding login}"/>
                <DataGridTextColumn Header="Пароль" Binding="{Binding password}"/>
                <DataGridTextColumn Header="Смена пароля" Binding="{Binding isPasswordChange}"/>
                <DataGridTextColumn Header="Заблокирован" Binding="{Binding blocked}"/>
                <DataGridTextColumn Header="Роль" Binding="{Binding role1.name}"/>
                <DataGridTextColumn Header="Последняя активность" Binding="{Binding last_activity}"/>
            </DataGrid.Columns>
        </DataGrid>
        <Button x:Name="AddButton" Content="Добавить" HorizontalAlignment="Left" Margin="22,223,0,0" VerticalAlignment="Top" Width="122" Height="33" Click="AddButton_Click" Grid.Row="1"/>
        <Button x:Name="EditButton" Content="Редактировать" HorizontalAlignment="Left" Margin="180,223,0,0" VerticalAlignment="Top" Width="122" Height="33" Click="EditButton_Click" Grid.Row="1"/>
        <Button x:Name="DeleteButton" Content="Удалить" HorizontalAlignment="Left" Margin="646,223,0,0" VerticalAlignment="Top" Width="122" Height="33" Click="DeleteButton_Click" Grid.Row="1"/>

    </Grid>
</Page>
